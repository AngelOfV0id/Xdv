/* This is a configuration file of Xdv - extreme directory navigator. See license.txt for copyright and licensing details. */

#define SHELL					"cmd.exe"									// Command shell
#define DISPLAY_FORMAT			"%.8d / %.8d:    %s > %s%s"					// Information display format [index, count, path, item type, item name]
#define DISPLAY_FORMAT_EMPTY	"%s > Nothing to see here. Sad but true."	// Information display format [path]. The same as DISPLAY_FORMAT, but for empty folder
#define DIRECTORY_MARK			"[DIR] "									// Directory mark (with space at the end)
